# ZAI FLOW 2.0 - Netlify Redirects for SPA Routing
# Routes all non-file requests to login.html for client-side app routing
# Static files (CSS, JS, images) are served directly by Netlify

# SPA catch-all: rewrite all requests to login.html with 200 status
# This enables client-side routing for all pages
/*  /login.html  200
